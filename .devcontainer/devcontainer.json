{
    "service": "app",
    "name": "contaienr 1",
    "dockerComposeFile": "docker-compose.yml",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "golang.go"
            ]
        }
    }
}